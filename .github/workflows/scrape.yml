# Skapad: 2025-07-29 23:59
name: Automatiskt hämta loppisdata

on:
  schedule:
    - cron: '59 23 * * *'  # Körs varje dag 23:59
  workflow_dispatch:       # Möjliggör manuell körning

jobs:
  run-scraper:
    runs-on: ubuntu-latest

    steps:
      - name: Klona repot
        uses: actions/checkout@v3

      - name: Installera Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Installera beroenden
        run: |
          pip install requests beautifulsoup4

      - name: Kör scraping-scriptet
        run: python scrape_loppistajm.py

      - name: Committa och pusha resultat
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add loppisar.json
          git commit -m "🗓 Auto-uppdaterad loppisar.json $(date +'%Y-%m-%d %H:%M')" || echo "Inget nytt att committa"
          git push
