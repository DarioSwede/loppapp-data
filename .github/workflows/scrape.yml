# .github/workflows/scrape.yml
# Skapad: 2025-07-29 23:59
name: Automatiskt hämta loppisdata

on:
  schedule:
    - cron: '59 23 * * *'  # Kör varje dag kl 23:59 UTC
  workflow_dispatch:       # Tillåter manuell start

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Klona repo
        uses: actions/checkout@v3

      - name: Installera Python och beroenden
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Installera paket
        run: pip install beautifulsoup4 requests

      - name: Kör scraping-script och logga
        run: |
          echo "🕒 Kör scraping $(date)" > scrape.log
          python scrape_loppistajm.py >> scrape.log 2>&1
          echo "✅ Klar $(date)" >> scrape.log

      - name: Commit & push JSON + logg
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add loppisar.json scrape.log
          git commit -m "🤖 Automatisk uppdatering $(date +'%Y-%m-%d %H:%M')" || echo "Inga ändringar"
          git push

  notify:
    needs: scrape
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Skicka e-post vid fel
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "❌ LoppApp scraping misslyckades"
          to: darioswede@gmail.com
          from: LoppApp Bot <${{ secrets.SMTP_USERNAME }}>
          body: |
            Scraping av loppisar misslyckades.

            Repo: https://github.com/DarioSwede/loppapp-data/actions

            Datum: ${{ github.event.head_commit.timestamp }}
