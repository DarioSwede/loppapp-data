# .github/workflows/scrape.yml
# Skapad: 2025-07-29 23:59
name: Automatiskt hämta loppisdata

on:
  schedule:
    - cron: '59 23 * * *'  # Kör varje dag kl 23:59 UTC (01:59 svensk sommartid)
  workflow_dispatch:       # Gör det också manuellt startbart från GitHub UI

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Klona repo
        uses: actions/checkout@v3

      - name: Installera Python och beroenden
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Installera paket
        run: pip install beautifulsoup4 requests

      - name: Kör scraping-script
        run: python scrape_loppistajm.py

      - name: Commit & push uppdaterad JSON
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add loppisar.json
          git commit -m "🤖 Automatisk uppdatering $(date +'%Y-%m-%d %H:%M')" || echo "Inga ändringar"
          git push
